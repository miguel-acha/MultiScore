# Copy to .env.local for local dev, or set VITE_API_URL as an environment
# variable in the Vercel project settings pointing to the deployed Cloud
# Run URL (e.g. https://multiscore-api-xxxxx-uc.a.run.app).
VITE_API_URL=http://localhost:8080
