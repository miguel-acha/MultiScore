# Pinned to match the exact versions used to train the models (see
# pyproject.toml / uv.lock) - loading a joblib-pickled sklearn/xgboost/
# lightgbm model with a different minor version can silently break or
# raise, so training and serving environments must match.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pandas==3.0.5
numpy==1.26.4
scikit-learn==1.9.1
xgboost==3.4.1
lightgbm==4.7.0
joblib==1.6.0
pyarrow==25.0.1
